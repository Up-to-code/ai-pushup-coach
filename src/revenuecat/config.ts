export const REVENUECAT_API_KEY = process.env.EXPO_PUBLIC_REVENUECAT_API_KEY ?? '';

export const PRO_ENTITLEMENT_ID = 'pushup_coach_pro';

export const PRODUCT_IDENTIFIERS = {
  lifetime: 'lifetime',
  yearly: 'yearly',
  monthly: 'monthly',
} as const;

export const DEFAULT_OFFERING_ID = 'default';
